{
    "name": "reproducer/phpstan-callable-array-memory-spike",
    "description": "Minimal standalone reproducer for PHPStan memory spike on 2-element array constants matching declared class names",
    "type": "project",
    "require": {
        "php": ">=8.2"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
